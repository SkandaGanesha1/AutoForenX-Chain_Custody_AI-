name: mobile_app

description: A mobile app for evidence collection in ChainCustody.ai.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0  # Updated from 0.15.0 which is very old
  local_auth: ^2.1.6  # Compatible version
  local_auth_windows: ^1.0.8  # Compatible version
  provider: ^6.0.5  # Corrected version
  file_picker: ^5.3.3
  intl: ^0.18.0
  crypto: ^3.0.2
  cupertino_icons: ^1.0.2  # Updated to stable version

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2  # Updated to stable version

flutter:
  uses-material-design: true